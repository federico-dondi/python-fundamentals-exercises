# 27 - Given height and width via input, calculate the area of the associated rectangle.

b = float(input("Enter the width: "))
h = float(input("Enter the height: "))

area = b * h

print(f"The area is: {area}")